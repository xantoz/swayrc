Link the wanted host config as config in to this directory.

Like this:

  $ ln -s host/usbee/config config

TODO: Maybe in the future we can make a config file that is smart enough that it does this automatically based on hostname?
