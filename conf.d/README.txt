Link the config snippets you would like to use in here. You probably
want at least the env, base and swaybar conf, and perhaps some host-specific conf.

E.g.:
  ln -s ../conf.avail/{00-env,50-base,55-swaybar}.conf ./
  ln -s ../host/akindestam-suse/*.conf ./
